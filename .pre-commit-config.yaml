repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: debug-statements

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.9
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: mypy
        name: mypy
        entry: uv run mypy app/ tests/
        language: system
        types: [python]
        pass_filenames: false

      - id: pytest-check
        name: pytest (unit + integration)
        entry: uv run pytest tests/unit tests/integration -x -q
        language: system
        types: [python]
        pass_filenames: false
        stages: [pre-push]

      - id: eslint
        name: ESLint (TypeScript/React)
        entry: bash -c 'cd webapp && [ -d node_modules ] && npx eslint . || true'
        language: system
        files: ^webapp/.*\.(ts|tsx|js|jsx)$
        pass_filenames: false

      - id: typescript-check
        name: TypeScript Check
        entry: bash -c 'cd webapp && [ -d node_modules ] && npx tsc --noEmit || true'
        language: system
        files: ^webapp/.*\.(ts|tsx)$
        pass_filenames: false
